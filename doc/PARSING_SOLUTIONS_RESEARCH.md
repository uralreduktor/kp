# Исследование решений для извлечения ИНН и данных с торговых площадок

**Дата**: 5 декабря 2025 г.  
**Контекст**: Проект KP (Коммерческие предложения) - система парсинга данных с B2B торговых площадок

---

## 📋 Содержание

1. [Текущее состояние проекта](#текущее-состояние-проекта)
2. [Обзор подходов к извлечению данных](#обзор-подходов-к-извлечению-данных)
3. [API-сервисы для получения реквизитов](#api-сервисы-для-получения-реквизитов)
4. [Технологии парсинга веб-страниц](#технологии-парсинга-веб-страниц)
5. [Готовые решения и сервисы](#готовые-решения-и-сервисы)
6. [Рекомендации по улучшению](#рекомендации-по-улучшению)
7. [План внедрения](#план-внедрения)

---

## 🔍 Текущее состояние проекта

### Архитектура
- **Backend**: PHP (Legacy) + FastAPI (New)
- **Парсер**: Гибридный подход (cURL + Playwright)
- **Паттерн**: Strategy с абстрактными коннекторами
- **Язык парсеров**: Любой (PHP, Python, Node.js, Go, Rust) - выбор по задаче
- **Коннекторы**: 
  - `B2BCenterConnector` - основной коннектор для B2B-Center
  - `GenericConnector` - универсальный парсер
  - `AbstractConnector` - базовый класс с общей логикой

### Текущие методы извлечения ИНН

#### 1. **Прямой парсинг со страницы тендера**
```php
// Метод 1: XPath поиск
$innNodes = $xpath->query("//*[contains(text(), 'ИНН')]/following-sibling::td[1]");

// Метод 2: Regex по HTML
preg_match('/ИНН[:\s]*(\d{10}|\d{12})/', $html, $matches);

// Метод 3: JSON в __NEXT_DATA__ (для Next.js страниц)
preg_match('/<script id="__NEXT_DATA__".*?>(.*?)<\/script>/s', $html, $m);
```

#### 2. **Переход по ссылке на профиль компании**
```php
// Если на странице тендера нет ИНН, но есть ссылка на профиль
if (empty($data['recipientINN']) && !empty($organizerInfo['link'])) {
    $this->fetchFirmDetails($organizerInfo['link'], $data);
}
```

#### 3. **DaData API (Вспомогательное использование)**
```php
// ⚠️ ВАЖНО: DaData используется ТОЛЬКО для подсказок при ручном вводе
// НЕ используется как fallback при отсутствии ИНН на странице
// Парсер либо находит ИНН напрямую, либо возвращает пустое поле

// Файл: api/company-suggest.php (для автозаполнения в UI)
function findCompany($query) {
    // Запрос к DaData для подсказок при вводе названия компании
    // Используется только в интерфейсе, не в автопарсинге
}
```

### Текущие проблемы

1. **Надежность парсинга**: Зависит от структуры HTML каждой площадки
2. **Антибот защита**: Cloudflare, капчи, JS-обфускация
3. **Производительность**: Headless браузер медленный (fallback)
4. **Поддержка**: Изменения в вёрстке ломают парсеры
5. **Охват**: Поддержка только нескольких площадок
6. **Принцип "всё или ничего"**: ИНН либо парсится напрямую, либо остаётся пустым (без внешних API-догадок)

---

## 🔧 Обзор подходов к извлечению данных

### 1. Direct Parsing (Прямой парсинг)

**Текущий статус**: ✅ Используется

**Технологии** (выбор языка программирования свободный):
- **PHP**: cURL + XPath (текущая реализация)
- **Node.js**: Playwright/Puppeteer (для JS-рендеринга)
- **Python**: Scrapy, BeautifulSoup, Playwright (мощный выбор для сложных сценариев)
- **Go**: Colly, chromedp (высокая производительность)
- **Rust**: scraper, fantoccini (максимальная скорость)
- **Selenium**: универсальный (legacy, ресурсоёмко)

**Преимущества**:
- ✅ Полный контроль над процессом
- ✅ Бесплатно (кроме инфраструктуры)
- ✅ Доступ к любым данным на странице

**Недостатки**:
- ❌ Требует постоянной поддержки
- ❌ Легко ломается при изменениях вёрстки
- ❌ Сложность обхода антибот защиты
- ❌ Риски блокировки IP

**Рекомендации по улучшению (мультиязычный подход)**:

**Node.js** (текущий выбор для Playwright):
```javascript
// Использование playwright-stealth для обхода детектирования
const { chromium } = require('playwright-extra');
const stealth = require('puppeteer-extra-plugin-stealth')();

const browser = await chromium.launch({
  headless: true,
  args: ['--no-sandbox', '--disable-blink-features=AutomationControlled']
});
```

**Python** (альтернатива, мощнее для ML/AI):
```python
from playwright.sync_api import sync_playwright
from playwright_stealth import stealth_sync

with sync_playwright() as p:
    browser = p.chromium.launch(headless=True)
    page = browser.new_page()
    stealth_sync(page)  # Обход детектирования
    page.goto("https://example.com")
```

**Go** (для высоконагруженных парсеров):
```go
import "github.com/chromedp/chromedp"

ctx, cancel := chromedp.NewContext(context.Background())
defer cancel()

var html string
chromedp.Run(ctx,
    chromedp.Navigate("https://example.com"),
    chromedp.OuterHTML("html", &html),
)
```

**Выбор языка зависит от задачи**: PHP/Node.js для простых сценариев, Python для ML-анализа, Go/Rust для производительности.

### 2. API Integration (Интеграция через API)

**Текущий статус**: ⚠️ Ограниченно (только DaData)

#### 2.1 DaData (Только для UI-подсказок)

**⚠️ ВАЖНОЕ УТОЧНЕНИЕ**: DaData НЕ используется как fallback при парсинге. Только для интерактивных подсказок в интерфейсе.

**Использование**:
```php
// Файл: api/company-suggest.php
// Используется ТОЛЬКО когда пользователь вводит название компании в UI
$url = 'https://suggestions.dadata.ru/suggestions/api/4_1/rs/suggest/party';
$data = [
    'query' => $companyName,  // От пользователя, не от парсера
    'count' => 5,
    'status' => ['ACTIVE']
];
```

**Сценарии использования**:
1. ✅ Пользователь вводит название компании → получает подсказки с ИНН
2. ✅ Пользователь вводит неполное название → автодополнение
3. ❌ **НЕ используется**: Парсер не нашёл ИНН → автоматический запрос к DaData

**Стоимость**: 
- Бесплатно: 10,000 запросов/день
- Стандарт: 30,000 запросов/день - 990₽/мес
- Бизнес: 100,000 запросов/день - 2,490₽/мес

**Преимущества**:
- ✅ Высокая точность (официальные данные ЕГРЮЛ)
- ✅ Полные реквизиты (ИНН, КПП, ОГРН, адрес)
- ✅ Помогает пользователю при ручном вводе

**Принцип работы парсера**:
```
Парсинг → Найден ИНН? → ДА: возвращаем ИНН
                      → НЕТ: возвращаем пустое поле (без DaData!)
```

#### 2.2 Контур.Фокус

**Текущий статус**: ❌ Не используется

**Возможности**:
- API получения реквизитов по ИНН/названию
- Расширенная информация (финансы, связи, риски)
- Проверка благонадёжности контрагентов

**Стоимость**: От 3,000₽/мес (тарифы 2024)

**Рекомендация**: Рассмотреть для корпоративного использования

#### 2.3 ФНС (Налоговая служба)

**Официальный сервис**: https://egrul.nalog.ru/

**Варианты доступа**:
1. **Бесплатный веб-сервис** (единичные запросы)
   - Выписки в PDF с ЭЦП
   - Ограничение: ручной ввод, капчи
   
2. **Платный API** (массовая выгрузка)
   - 150,000₽/год за ЕГРЮЛ
   - 150,000₽/год за ЕГРИП
   - Итого: 300,000₽/год за оба реестра

**Рекомендация**: ❌ Не подходит (слишком дорого для проекта)

#### 2.4 API торговых площадок

**B2B-Center**:
- ❌ Официальное API отсутствует (открытые источники)
- ⚠️ Возможно наличие закрытого API для партнёров (требует уточнения)

**Zakupki.gov.ru** (ЕИС):
- ✅ **Есть официальный API** (с 1 января 2025)
- 📍 Требуется регистрация и получение токена
- 🔗 Документация: https://zakupki.gov.ru/epz/opendata/
- 💡 **Рекомендуется изучить** для парсинга госзакупок

**Другие площадки**:
- Большинство не предоставляют публичного API
- Требуется прямой парсинг

### 3. OCR (Оптическое распознавание)

**Текущий статус**: ❌ Не используется

**Применимость**: Извлечение ИНН из:
- Отсканированных документов
- PDF счетов/договоров
- Скриншотов

**Технологии**:

#### Tesseract OCR (Open Source)
```python
import pytesseract
from PIL import Image
import re

# Распознавание текста
text = pytesseract.image_to_string(Image.open('invoice.png'), lang='rus')

# Извлечение ИНН
inn_match = re.search(r'ИНН[:\s]*(\d{10}|\d{12})', text)
```

**Преимущества**:
- ✅ Бесплатно
- ✅ Open Source
- ✅ Поддержка русского языка

**Недостатки**:
- ❌ Требует качественных изображений
- ❌ Низкая точность на плохих сканах
- ❌ Нужна предобработка (денойз, бинаризация)

#### ABBYY FineReader Engine/Cloud

**Преимущества**:
- ✅ Высокая точность (95%+)
- ✅ Распознавание структуры документа
- ✅ Облачный API доступен

**Недостатки**:
- ❌ Платный (от ~$1,000 за SDK)
- ❌ Cloud API - per-page billing

**Рекомендация**: Использовать только если в проекте появится требование обработки документов

---

## 🌐 Технологии парсинга веб-страниц

### Текущий стек

#### 1. PHP cURL (Fast Path)
```php
// Файл: api/connectors/AbstractConnector.php
protected function request($url, $postData = [], $headers = []) {
    $ch = curl_init();
    curl_setopt_array($ch, [
        CURLOPT_URL => $url,
        CURLOPT_RETURNTRANSFER => true,
        CURLOPT_FOLLOWLOCATION => true,
        CURLOPT_TIMEOUT => 30,
        // ... реалистичные заголовки
    ]);
    return curl_exec($ch);
}
```

**Проблемы**:
- Не выполняет JavaScript
- Легко детектируется как бот
- Не проходит Cloudflare/капчи

#### 2. Playwright (Fallback)
```javascript
// Файл: api/parse_html_playwright.js
const browser = await chromium.launch({
    headless: true,
    args: ['--no-sandbox', '--disable-setuid-sandbox']
});

await page.goto(url, { waitUntil: 'domcontentloaded', timeout: 60000 });
await page.waitForTimeout(2000);
```

**Проблемы**:
- Простой запуск (легко детектируется)
- Нет stealth-плагинов
- Нет ротации fingerprints

### Рекомендуемые улучшения

#### 1. Playwright Stealth (Priority: HIGH)

**Установка**:
```bash
npm install playwright-extra puppeteer-extra-plugin-stealth
```

**Интеграция**:
```javascript
const { chromium } = require('playwright-extra');
const stealth = require('puppeteer-extra-plugin-stealth')();

chromium.use(stealth);

const browser = await chromium.launch({
  headless: true,
  args: [
    '--no-sandbox',
    '--disable-blink-features=AutomationControlled',
    '--disable-dev-shm-usage'
  ]
});

const context = await browser.newContext({
  viewport: { width: 1920, height: 1080 },
  userAgent: 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36',
  locale: 'ru-RU',
  timezoneId: 'Europe/Moscow'
});

// Маскировка webdriver
await context.addInitScript(() => {
  Object.defineProperty(navigator, 'webdriver', { get: () => false });
  window.chrome = { runtime: {} };
});
```

**Новый файл**: `api/parse_html_playwright_stealth.js`

#### 2. Эмуляция поведения пользователя

```javascript
// Случайные задержки
function randomDelay(min = 1000, max = 3000) {
  return new Promise(resolve => 
    setTimeout(resolve, Math.random() * (max - min) + min)
  );
}

// Эмуляция движения мыши
async function humanMouseMove(page, selector) {
  const element = await page.$(selector);
  const box = await element.boundingBox();
  
  // Плавное движение мыши к элементу
  await page.mouse.move(box.x + box.width / 2, box.y + box.height / 2, {
    steps: 10 + Math.floor(Math.random() * 10)
  });
  
  await randomDelay(100, 300);
}

// Случайная прокрутка
async function randomScroll(page) {
  const scrollHeight = await page.evaluate(() => document.body.scrollHeight);
  const viewportHeight = await page.evaluate(() => window.innerHeight);
  
  const scrollTo = Math.random() * (scrollHeight - viewportHeight);
  await page.evaluate((y) => window.scrollTo(0, y), scrollTo);
  
  await randomDelay(500, 1500);
}
```

#### 3. Ротация прокси и User-Agent

**Прокси-сервисы**:
- Bright Data (бывший Luminati) - от $500/мес
- Oxylabs - от $300/мес
- SmartProxy - от $75/мес
- Российские: ProxyLine, ProxySeller (от 500₽/мес)

**Интеграция**:
```javascript
const proxies = [
  'http://user:pass@proxy1.com:8080',
  'http://user:pass@proxy2.com:8080',
  // ...
];

const userAgents = [
  'Mozilla/5.0 (Windows NT 10.0; Win64; x64) Chrome/120.0.0.0',
  'Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_7) Safari/537.36',
  // ...
];

const proxy = proxies[Math.floor(Math.random() * proxies.length)];
const userAgent = userAgents[Math.floor(Math.random() * userAgents.length)];

const context = await browser.newContext({
  proxy: { server: proxy },
  userAgent: userAgent
});
```

#### 4. Обработка капч

**Anti-Captcha сервисы**:
- 2Captcha - $2.99 за 1000 капч
- Anti-Captcha - $3 за 1000 капч
- RuCaptcha - от 80₽ за 1000 капч

**Интеграция**:
```javascript
const AntiCaptcha = require('@antiadmin/anticaptchaofficial');

async function solveCaptcha(pageUrl, siteKey) {
  const ac = new AntiCaptcha('YOUR_API_KEY');
  
  const taskId = await ac.createTask({
    type: 'RecaptchaV2TaskProxyless',
    websiteURL: pageUrl,
    websiteKey: siteKey
  });
  
  const solution = await ac.getTaskResult(taskId);
  return solution.gRecaptchaResponse;
}

// Использование
if (await page.$('.g-recaptcha')) {
  const siteKey = await page.$eval('.g-recaptcha', el => el.dataset.sitekey);
  const solution = await solveCaptcha(page.url(), siteKey);
  
  await page.evaluate((token) => {
    document.getElementById('g-recaptcha-response').value = token;
  }, solution);
}
```

---

## 📦 Готовые решения и сервисы

### 1. Zakupki.gov.ru парсеры

#### EIS Parser от SIP-Projects
- **Описание**: Готовый парсер госзакупок с аналитикой
- **Функционал**:
  - Автозагрузка тендеров по критериям
  - Фильтрация и аналитика
  - Экспорт в Excel
- **Стек**: Python + PostgreSQL + Symfony
- **Стоимость**: По запросу (корпоративное решение)
- **Сайт**: https://sip-projects.com/projects/eis

#### Мультитендер API
- **Описание**: API к структурированной базе госзакупок
- **Данные**: Закупки, контракты, поставщики
- **Стоимость**: По запросу
- **Сайт**: https://multitender.ru/products/api-k-dannym/

#### ExcelVBA Parser
- **Описание**: Парсер на VBA для Excel
- **Функционал**: Сбор данных о закупках в Excel
- **Стоимость**: Бесплатно (базовая версия)
- **Сайт**: https://excelvba.ru/programmes/Parser/samples/zakupki

### 2. B2B-Center решения

#### Интеграция SAP + B2B-Center (Fix Price кейс)
- **Описание**: Корпоративная интеграция ERP с ЭТП
- **Функционал**:
  - Автосоздание заявок на закупку
  - Формирование процедур на ЭТП
  - Согласование контрактов
- **Стек**: SAP + B2B-Center API (закрытый)
- **Применимость**: Только для крупных компаний

**Вывод**: Для малого/среднего бизнеса доступны только методы парсинга.

### 3. Битрикс24 приложения

#### Парсер госзакупок от OPTI
- **Описание**: Интеграция zakupki.gov.ru в CRM Битрикс24
- **Функционал**: Автосоздание сделок из тендеров
- **Стоимость**: ~5,000₽ (лицензия)
- **Применимость**: Только для пользователей Битрикс24

---

## 💡 Рекомендации по улучшению

### Приоритет 1: CRITICAL (Необходимо сейчас)

#### 1.1 Внедрение Playwright Stealth
**Задача**: Обновить `parse_html_playwright.js` с stealth-плагинами

**Действия**:
```bash
cd /var/www/kp/api
npm install playwright-extra puppeteer-extra-plugin-stealth
```

**Ожидаемый эффект**:
- ✅ Снижение блокировок на 60-80%
- ✅ Успешный обход Cloudflare (базовый уровень)
- ✅ Маскировка автоматизации

**Оценка**: 4-6 часов разработки

#### 1.2 Кеширование результатов парсинга
**Задача**: Избежать повторных запросов к площадкам

**Решение**:
```php
// Новый файл: api/cache/ParserCache.php
class ParserCache {
    private $cacheDir;
    private $ttl = 3600; // 1 час
    
    public function get($url) {
        $cacheFile = $this->getCacheFile($url);
        if (file_exists($cacheFile) && 
            (time() - filemtime($cacheFile)) < $this->ttl) {
            return json_decode(file_get_contents($cacheFile), true);
        }
        return null;
    }
    
    public function set($url, $data) {
        file_put_contents($this->getCacheFile($url), json_encode($data));
    }
    
    private function getCacheFile($url) {
        return $this->cacheDir . '/' . md5($url) . '.json';
    }
}
```

**Ожидаемый эффект**:
- ✅ Снижение нагрузки на площадки
- ✅ Ускорение повторных запросов в 100+ раз
- ✅ Меньше шансов на блокировку

**Оценка**: 2-3 часа разработки

#### 1.3 Улучшение логирования и мониторинга
**Задача**: Отслеживать успешность парсинга

**Решение**:
```php
// Обновить AbstractConnector.php
protected function logParsingMetrics($url, $success, $method, $duration) {
    $metrics = [
        'timestamp' => date('Y-m-d H:i:s'),
        'url' => $url,
        'domain' => parse_url($url, PHP_URL_HOST),
        'success' => $success,
        'method' => $method, // 'curl' или 'browser'
        'duration_ms' => $duration,
        'connector' => get_class($this)
    ];
    
    // Лог для анализа
    file_put_contents(
        __DIR__ . '/../logs/parsing_metrics.jsonl',
        json_encode($metrics) . "\n",
        FILE_APPEND
    );
}
```

**Ожидаемый эффект**:
- ✅ Понимание проблемных площадок
- ✅ Статистика успешности парсинга
- ✅ Данные для оптимизации

**Оценка**: 2 часа разработки

### Приоритет 2: HIGH (Важно в ближайшее время)

#### 2.1 Интеграция с Zakupki.gov.ru API
**Обоснование**: Госзакупки - большой сегмент, есть официальный API

**Действия**:
1. Регистрация на https://zakupki.gov.ru/epz/opendata/
2. Получение токена доступа
3. Создание `ZakupkiAPIConnector.php`

**Пример запроса**:
```php
class ZakupkiAPIConnector extends AbstractConnector {
    private $apiToken;
    private $apiUrl = 'https://zakupki.gov.ru/epz/opendata/api/v1/';
    
    public function parse($url) {
        // Извлекаем ID тендера из URL
        preg_match('/noticeId=(\d+)/', $url, $m);
        $noticeId = $m[1];
        
        // API запрос
        $response = $this->apiRequest("notices/{$noticeId}");
        
        // Парсинг JSON вместо HTML
        return [
            'tenderNumber' => $response['regNumber'],
            'recipient' => $response['customer']['fullName'],
            'recipientINN' => $response['customer']['inn'],
            // ... остальные поля
        ];
    }
    
    private function apiRequest($endpoint) {
        $headers = ['Authorization: Bearer ' . $this->apiToken];
        return json_decode($this->request($this->apiUrl . $endpoint, [], $headers), true);
    }
}
```

**Ожидаемый эффект**:
- ✅ 100% надёжность для госзакупок
- ✅ Нет проблем с блокировками
- ✅ Официальные данные (юридически значимые)

**Оценка**: 8-12 часов разработки + тестирование

#### 2.2 Расширение поддержки DaData
**Задача**: Использовать дополнительные возможности API

**Новые методы**:
```php
// Файл: api/services/DaDataService.php
class DaDataService {
    // Уже есть: поиск по названию
    public function suggestCompany($query) { /* ... */ }
    
    // НОВОЕ: Стандартизация адреса
    public function cleanAddress($address) {
        $url = 'https://cleaner.dadata.ru/api/v1/clean/address';
        // ...
        return $normalized;
    }
    
    // НОВОЕ: Определение банковских реквизитов
    public function findBank($bik) {
        $url = 'https://suggestions.dadata.ru/suggestions/api/4_1/rs/suggest/bank';
        // ...
    }
    
    // НОВОЕ: Обогащение данных компании
    public function enrichCompany($inn) {
        $url = 'https://suggestions.dadata.ru/suggestions/api/4_1/rs/findById/party';
        $data = ['query' => $inn];
        // Получаем: ОКВЭД, даты регистрации, капитал, директора
        // ...
    }
}
```

**Ожидаемый эффект**:
- ✅ Более полные данные о компаниях
- ✅ Нормализованные адреса
- ✅ Дополнительная проверка благонадёжности

**Оценка**: 4-6 часов разработки

#### 2.3 Ротация User-Agent и базовая рандомизация
**Задача**: Снизить детектирование бота в cURL

**Решение**:
```php
// Файл: api/utils/UserAgentRotator.php
class UserAgentRotator {
    private static $userAgents = [
        'Mozilla/5.0 (Windows NT 10.0; Win64; x64) Chrome/120.0.0.0 Safari/537.36',
        'Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_7) Safari/605.1.15',
        'Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:121.0) Firefox/121.0',
        'Mozilla/5.0 (X11; Linux x86_64) Chrome/120.0.0.0 Safari/537.36',
        // ... добавить ещё 10-15 реалистичных
    ];
    
    public static function getRandom() {
        return self::$userAgents[array_rand(self::$userAgents)];
    }
    
    public static function getRandomDelay($min = 1, $max = 3) {
        return mt_rand($min * 1000000, $max * 1000000);
    }
}

// В AbstractConnector.php
protected function request($url, $postData = [], $headers = []) {
    // Случайная задержка перед запросом
    usleep(UserAgentRotator::getRandomDelay(0.5, 2));
    
    // Случайный User-Agent
    $defaultHeaders = [
        'User-Agent: ' . UserAgentRotator::getRandom(),
        // ...
    ];
    // ...
}
```

**Ожидаемый эффект**:
- ✅ Снижение блокировок на 20-30%
- ✅ Имитация разных пользователей

**Оценка**: 2-3 часа разработки

### Приоритет 3: MEDIUM (Планировать на будущее)

#### 3.1 Интеграция прокси-сервиса
**Обоснование**: Для масштабирования и обхода жёстких блокировок

**Рекомендуемые сервисы**:
- **ProxyLine** (РФ): от 500₽/мес, 1000 запросов/день
- **SmartProxy**: от $75/мес, безлимит (residential)
- **Bright Data**: от $500/мес, enterprise (рекомендуется для больших объёмов)

**Интеграция**:
```php
// api/config.php
$config['proxy'] = [
    'enabled' => true,
    'provider' => 'proxyline',
    'servers' => [
        'http://user:pass@proxy1.proxyline.net:10000',
        'http://user:pass@proxy2.proxyline.net:10000',
    ],
    'rotation' => 'random' // или 'round-robin'
];

// В AbstractConnector.php
if ($config['proxy']['enabled']) {
    $proxy = $this->getNextProxy();
    curl_setopt($ch, CURLOPT_PROXY, $proxy);
    curl_setopt($ch, CURLOPT_PROXYTYPE, CURLPROXY_HTTP);
}
```

**Ожидаемый эффект**:
- ✅ Практически нет риска блокировки по IP
- ✅ Возможность парсить тысячи страниц в день

**Оценка**: 6-8 часов разработки

#### 3.2 ML-модель для извлечения данных
**Обоснование**: Универсальное решение для любых площадок

**Подход**:
- Обучить модель на примерах (Named Entity Recognition)
- Входные данные: HTML страниц тендеров
- Выходные данные: Структурированные поля (ИНН, название, адрес)

**Технологии**:
- Python: spaCy, Hugging Face Transformers
- Модель: BERT для русского языка (DeepPavlov RuBERT)

**Пример**:
```python
import spacy

# Загрузка модели
nlp = spacy.load("ru_core_news_lg")

# Обучение на примерах
TRAIN_DATA = [
    ("ИНН: 1234567890 ООО Компания", {"entities": [(5, 15, "INN"), (16, 28, "ORG")]}),
    # ... больше примеров
]

# Использование
doc = nlp(html_text)
for ent in doc.ents:
    if ent.label_ == "INN":
        inn = ent.text
```

**Ожидаемый эффект**:
- ✅ Универсальность (работает на любых площадках)
- ✅ Устойчивость к изменениям вёрстки
- ✅ Автоматическое улучшение с накоплением данных

**Недостатки**:
- ❌ Требует большого датасета для обучения (500+ примеров)
- ❌ Сложность внедрения (Python интеграция)
- ❌ Вычислительные ресурсы (GPU желательно)

**Оценка**: 40-60 часов (R&D проект)

**Рекомендация**: Рассматривать только если проект масштабируется на 10+ площадок

#### 3.3 Сервис решения капч
**Обоснование**: Автоматизация при появлении капч

**Стоимость**:
- RuCaptcha: 80₽ за 1000 капч
- 2Captcha: ~250₽ за 1000 капч

**Интеграция**:
```php
// api/services/CaptchaSolver.php
class CaptchaSolver {
    private $apiKey;
    
    public function solveRecaptcha($siteKey, $pageUrl) {
        // Отправка задачи
        $taskId = $this->createTask([
            'type' => 'NoCaptchaTaskProxyless',
            'websiteURL' => $pageUrl,
            'websiteKey' => $siteKey
        ]);
        
        // Ожидание решения (обычно 10-30 сек)
        return $this->waitForResult($taskId);
    }
}
```

**Ожидаемый эффект**:
- ✅ Автоматический обход капч
- ✅ Парсинг защищённых площадок

**Оценка**: 4-6 часов разработки

---

## 📊 Сравнительная таблица решений

| Решение | Надёжность | Стоимость | Сложность | Применимость | Рекомендация |
|---------|-----------|-----------|-----------|--------------|--------------|
| **Прямой парсинг (cURL)** | ⭐⭐ | Бесплатно | Низкая | Простые сайты | ✅ Базовый метод |
| **Playwright базовый** | ⭐⭐⭐ | Бесплатно | Средняя | JS-сайты | ✅ Текущий fallback |
| **Playwright Stealth** | ⭐⭐⭐⭐ | Бесплатно | Средняя | Защищённые сайты | 🔥 CRITICAL |
| **DaData API** | ⭐⭐⭐⭐⭐ | 990₽/мес | Низкая | Подтверждение ИНН | ✅ Используется |
| **Контур.Фокус** | ⭐⭐⭐⭐⭐ | 3000₽/мес | Низкая | Расширенные данные | ⚠️ Опционально |
| **ФНС API** | ⭐⭐⭐⭐⭐ | 300k₽/год | Средняя | Массовая выгрузка | ❌ Слишком дорого |
| **Zakupki API** | ⭐⭐⭐⭐⭐ | Бесплатно | Средняя | Госзакупки | 🔥 HIGH |
| **Прокси-сервис** | ⭐⭐⭐⭐ | 500₽/мес | Средняя | Масштабирование | ⚠️ При росте |
| **Антикапча** | ⭐⭐⭐⭐ | 80₽/1000 | Низкая | Капчи | ⚠️ По требованию |
| **ML парсинг** | ⭐⭐⭐⭐ | Бесплатно | Высокая | Универсальность | ❌ Только для 10+ площадок |

---

## 📅 План внедрения

### Фаза 1: Быстрые улучшения (1-2 недели)

**Неделя 1**:
- ✅ Внедрение Playwright Stealth (6 часов)
- ✅ Кеширование результатов (3 часа)
- ✅ Улучшенное логирование (2 часа)
- ✅ User-Agent ротация (3 часа)

**Итого**: 14 часов разработки

**Результат**:
- Снижение блокировок на 50-70%
- Ускорение повторных запросов
- Данные для анализа проблем

### Фаза 2: API интеграции (2-3 недели)

**Неделя 2-3**:
- ✅ Zakupki.gov.ru API (12 часов)
- ✅ Расширение DaData (6 часов)
- ✅ Рефакторинг ConnectorFactory (4 часа)

**Итого**: 22 часа разработки

**Результат**:
- Поддержка госзакупок через API
- Более полные данные компаний
- Готовность к новым коннекторам

### Фаза 3: Масштабирование (по необходимости)

**При росте нагрузки**:
- Интеграция прокси-сервиса (8 часов)
- Сервис решения капч (6 часов)
- Распределённый парсинг (queue-based)

**При расширении на 5+ площадок**:
- Исследование ML-подхода (R&D: 60 часов)

---

## 🎯 Метрики успеха

### Текущие показатели (оценка)
- ✅ Успешность парсинга: ~60-70%
- ⚠️ Блокировки: ~20-30% запросов
- ⚠️ Время парсинга: 5-15 сек (cURL), 20-40 сек (browser)
- ❌ Поддерживаемых площадок: 2-3

### Целевые показатели (после фазы 1-2)
- ✅ Успешность парсинга: **85-90%**
- ✅ Блокировки: **<10%** запросов
- ✅ Время парсинга: **2-5 сек** (cache), 5-10 сек (cURL), 15-25 сек (browser)
- ✅ Поддерживаемых площадок: **5-7**

### KPI для мониторинга
```sql
-- Пример запроса для анализа логов
SELECT 
    domain,
    COUNT(*) as total_requests,
    SUM(CASE WHEN success = 1 THEN 1 ELSE 0 END) as successful,
    ROUND(AVG(duration_ms)) as avg_duration_ms,
    method
FROM parsing_metrics
WHERE timestamp >= DATE_SUB(NOW(), INTERVAL 7 DAY)
GROUP BY domain, method
ORDER BY total_requests DESC;
```

---

## 🔗 Полезные ссылки

### Документация
- [Playwright Docs](https://playwright.dev/docs/intro)
- [DaData API](https://dadata.ru/api/)
- [Zakupki.gov.ru API](https://zakupki.gov.ru/epz/opendata/)

### Инструменты
- [2Captcha](https://2captcha.com/)
- [ProxyLine](https://proxyline.net/)
- [Tesseract OCR](https://github.com/tesseract-ocr/tesseract)

### Статьи
- [Stealth Web Scraping 2025](https://moonlightdevs.com/blog/stealth-web-scraping-2025-python-proxies-c3d2e6b4-3b89-4d93-a8b6-0960073bcef0)
- [Обход Cloudflare с Playwright](https://www.youtube.com/watch?v=h0WJS4CRtPc)

### Внутренняя документация проекта
- `/var/www/kp/.cursor/rules/parsing_rules.mdc` - Правила разработки парсеров
- `/var/www/kp/doc/DADATA_INTEGRATION.md` - Интеграция DaData (если есть)

---

## 📝 Выводы

### Главные выводы исследования

1. **Текущий подход работает**, но требует улучшения надёжности
2. **Playwright Stealth** - критически важное обновление (можно внедрить за 1 день)
3. **DaData используется ТОЛЬКО для UI-подсказок**, не как fallback при парсинге
4. **ИНН парсится напрямую или остаётся пустым** - принцип "всё или ничего"
5. **Zakupki.gov.ru API** - большая возможность (официальный источник)
6. **Выбор языка программирования свободный** - PHP, Python, Node.js, Go, Rust
7. **Прокси и капчи** - нужны только при масштабировании

### Принципы парсинга ИНН

```
┌─────────────────┐
│  Загрузка HTML  │
└────────┬────────┘
         │
    ┌────▼─────┐
    │ XPath    │───► Найден ИНН? ──YES──► ✅ Возврат ИНН
    │ Regex    │                  │
    │ JSON     │                 NO
    └────┬─────┘                  │
         │                        │
    ┌────▼────────────┐           │
    │ Переход по      │           │
    │ ссылке профиля  │───► Найден ИНН? ──YES──► ✅ Возврат ИНН
    └────┬────────────┘                  │
         │                              NO
         │                               │
         ▼                               │
    ❌ Возврат пустого поля ◄────────────┘
    (БЕЗ запроса к DaData!)
```

### Рекомендуемые первые шаги

1. ✅ **Сегодня**: Внедрить Playwright Stealth (6 часов)
2. ✅ **Эта неделя**: Добавить кеширование и логирование (5 часов)
3. ✅ **Следующая неделя**: Интеграция Zakupki API (12 часов)
4. ⏳ **Через месяц**: Оценить результаты, планировать фазу 3

### Ожидаемый ROI

**Инвестиции**:
- Разработка фаз 1-2: ~36 часов
- Дополнительные сервисы: ~1,000₽/мес (DaData Стандарт)

**Результат**:
- ⬆️ Надёжность парсинга: +20-30%
- ⬇️ Блокировки: -50-70%
- ⬆️ Охват площадок: +2-4 площадки
- 💰 Снижение ручной работы: экономия 5-10 часов/неделя

---

**Документ подготовлен**: 5 декабря 2025 г.  
**Следующий пересмотр**: После внедрения фазы 1 (через 2 недели)
