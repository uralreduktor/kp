# 🔒 Установка SSL сертификата для kp.uralreduktor.com

## ⚠️ Проблема

При попытке установить сертификат для `www.kp.uralreduktor.com` возникла ошибка, так как DNS запись для www поддомена не настроена.

## ✅ Решение

Установите сертификат только для основного домена `kp.uralreduktor.com`:

```bash
sudo certbot --nginx -d kp.uralreduktor.com --non-interactive --agree-tos --register-unsafely-without-email --redirect
```

Или с указанием email (рекомендуется):

```bash
sudo certbot --nginx -d kp.uralreduktor.com --email your-email@example.com --agree-tos --redirect
```

## 📋 Что делает команда

- `--nginx` - автоматически настраивает nginx
- `-d kp.uralreduktor.com` - устанавливает сертификат только для основного домена
- `--redirect` - автоматически настраивает перенаправление HTTP → HTTPS
- `--agree-tos` - согласие с условиями использования Let's Encrypt
- `--register-unsafely-without-email` - регистрация без email (или укажите `--email`)

## 🔄 После установки

После успешной установки certbot автоматически:
1. Получит SSL сертификат от Let's Encrypt
2. Обновит конфигурацию nginx
3. Настроит автоматическое перенаправление HTTP → HTTPS
4. Перезагрузит nginx

## ✅ Проверка

После установки проверьте:

```bash
# Проверка конфигурации
sudo nginx -t

# Проверка сертификатов
sudo certbot certificates

# Проверка HTTPS
curl -I https://kp.uralreduktor.com
```

## 🌐 Добавление www поддомена в будущем

Если в будущем вы настроите DNS для `www.kp.uralreduktor.com`, вы сможете добавить его в сертификат:

```bash
sudo certbot --nginx -d kp.uralreduktor.com -d www.kp.uralreduktor.com --expand
```

---

**Примечание:** Конфигурация nginx уже обновлена и не содержит www поддомен.

