# 🎨 План редизайна системы КП v2.0
## ООО "Вектор" — Реестр коммерческих предложений

**Дата:** 3 декабря 2025
**Версия:** 2.0 (Vite Migration)
**Статус:** Актуализирован | **Прогресс:** ~65%

> 📊 **Подробный анализ прогресса:** см. [PROGRESS_ANALYSIS.md](./PROGRESS_ANALYSIS.md)

---

## 📊 1. Исследование бизнес-логики

### 1.1 Обзор системы
**Назначение:** Система создания, управления и генерации PDF коммерческих предложений.
**Текущая архитектура:**
- **Frontend:** React (CDN, no-build) + Tailwind (CLI) — *требует миграции на Vite*.
- **Auth:** FastAPI (Python) + JWT/Session.
- **Data:** PHP (Legacy API) + JSON files.
- **PDF:** Playwright (Node.js).

### 1.2 Пользовательские роли
| Роль | Email паттерн | Права |
|------|---------------|-------|
| **Администратор** | `*admin*` | Full access + Удаление |
| **Менеджер** | Обычный email | Создание, Редактирование |
| **Только чтение** | `*kka*` | Только просмотр |

---

## 🎯 2. Стратегия модернизации

### Проблема текущего решения (No-Build)
Текущая реализация через CDN (`<script type="text/babel">`) достигла предела масштабируемости:
1. Невозможно использовать современные библиотеки (Headless UI, React Query, Zod).
2. Сложность поддержки кода в одном файле.
3. Отсутствие типизации (TypeScript) повышает риск ошибок в сложных формах КП.

### Решение: Внедрение сборщика (Vite)
Переход на **Single Page Application (SPA)** с использованием Vite.
- **Стек:** Vite + React + TypeScript + Tailwind CSS.
- **Роутинг:** React Router (вместо отдельных файлов `.html`).
- **Стейт:** TanStack Query (кэширование API) + Zustand.
- **Формы:** React Hook Form + Zod (валидация схем КП).

---

## 🏗️ 3. Этапы реализации (Roadmap)

### 🏁 Фаза 0: Инфраструктура (1 неделя) ✅ **100%**
*Цель: Настроить среду разработки для SPA.*

1. **Инициализация проекта** ✅
   - ✅ Развернуть Vite + React + TypeScript в папке `/frontend`.
   - ✅ Настроить проксирование запросов `/api` к бэкенду (PHP/FastAPI).
2. **Настройка UI Kit** ✅
   - ✅ Подключить Tailwind CSS.
   - ✅ Настроить `cn` utility (clsx + tailwind-merge).
   - ⚠️ Базовые компоненты: Button, Input, Card, Modal (Headless UI используется напрямую).
3. **Аутентификация** ✅
   - ✅ Портировать `js/auth.js` в TypeScript сервис.
   - ✅ Реализовать AuthProvider (Context) для защиты роутов.

### 🚀 Фаза 1: Миграция Реестра (1 неделя) ✅ **100%**
*Цель: Перенести уже существующий функционал `index.html` на React компоненты.*
*Примечание: UI реестра уже готов на 90%, требуется только рефакторинг кода.*

1. **Компоненты Реестра** ✅
   - ✅ `InvoiceTable` (Десктоп) / адаптивная таблица для мобильных.
   - ✅ `FiltersPanel` (Сворачиваемые фильтры).
   - ✅ `StatsDashboard` (Виджеты).
2. **Логика данных** ✅
   - ✅ Замена `fetch` на `useQuery` (TanStack Query) для кэширования и авто-обновления.
   - ✅ Реализация пагинации и сортировки на клиенте.
3. **Результат:** ✅ Полностью функциональный реестр на url `/` (SPA).

### 📝 Фаза 2: Редактор КП (3 недели) 🔄 **~90%**
*Цель: Замена `pi.html`. Самая сложная часть системы.*

1. **Архитектура формы** ✅
   - ✅ Использовать **React Hook Form** для управления состоянием.
   - ✅ Валидация через **Zod** (схемы данных КП).
   - ✅ `AutoSave` хук (отправка черновика каждые 30 сек) — **реализовано**.
2. **UX улучшения (Wizard)** 🔄
   - ✅ **Stepper:** Навигация по шагам (Данные -> Позиции -> Условия).
   - ✅ **Smart Inputs:** Автокомплит ИНН (DaData), подстановка адресов — **реализовано**.
   - ❌ **Валидация шагов:** Проверка полей перед переходом — **в процессе**.
3. **Редактор позиций (Items)** ✅
   - ✅ Динамический список товаров (useFieldArray).
   - ✅ Калькулятор итогов в реальном времени.
   - ✅ Drag-and-drop сортировка позиций — **реализовано**.

### ⚖️ Фаза 3: Тендеры и PDF (2 недели) 🔄 **~50%**
*Цель: Специфика тендеров и генерация документов.*

1. **Тендерная форма** 🔄
   - ✅ Наследование от основной формы КП (через `documentType`).
   - ✅ Дополнительные поля (площадка, ID закупки, ссылка).
   - ❌ Импорт данных (парсинг ссылок) — **не реализовано**.
2. **PDF Preview** 🔄
   - ⚠️ Модальное окно предпросмотра (вместо split-screen).
   - ✅ Генерация PDF через существующий `generate_pdf_playwright.js`.
   - ❌ Split-screen режим: Форма слева, превью PDF справа — **не реализовано**.

### 💅 Фаза 4: Полировка и QA (1 неделя) ❌ **0%**
1. **Mobile Experience** ⚠️ Базовая адаптивность есть, финальная доводка не выполнена.
2. **Performance:** ❌ Code splitting, lazy loading маршрутов — **не реализовано**.
3. **Deployment:** ⚠️ Nginx настроен для SPA (`try_files`), но не для `dist/` из Vite — **требует доработки**.

---

## 🔧 4. Технические требования

### Frontend (New)
| Технология | Зачем нужна |
|------------|-------------|
| **Vite** | Быстрая сборка, HMR, поддержка модулей |
| **TypeScript** | Строгая типизация (критично для сложных структур КП) |
| **TanStack Query** | Управление серверным состоянием, кэширование, deduping |
| **React Hook Form** | Производительные формы без лишних ререндеров |
| **Zod** | Валидация данных (синхронизация с типами TS) |
| **Radix UI / Headless** | Доступные (a11y) UI примитивы (Dialog, Popover, Tabs) |

### Backend (Hybrid)
- **FastAPI:** Аутентификация, Proxy, новые эндпоинты валидации.
- **PHP (Legacy):** Оставляем для операций `list.php`, `save.php` (пока не будет ресурсов на переписывание). API остается совместимым.

### Инфраструктура
- `nginx`: Раздает статику из папки `dist/` (после сборки) и проксирует `/api`.
- `CI/CD`: Автоматическая сборка фронтенда при пуше.

---

## 📅 Обновленный Timeline

| Неделя | Фаза | Задачи |
|--------|------|--------|
| **1** | Фаза 0 | Init Vite, TS Setup, UI Kit, Auth |
| **2** | Фаза 1 | Портирование Реестра (Dashboard), React Router |
| **3-4** | Фаза 2 | Форма КП (Core), Валидация, Автосохранение |
| **5** | Фаза 2+3 | Редактор позиций, Тендеры, PDF Preview |
| **6** | Фаза 4 | Тестирование, Мобильные правки, Релиз |

**Итого:** 6 недель до полного релиза новой версии.

---

## ⚠️ Риски и Митигация

1. **Сложность настройки Nginx:**
   - *Решение:* SPA требует настройки `try_files $uri /index.html` для роутинга. Подготовить конфиг заранее.
2. **Дублирование логики валидации:**
   - *Решение:* Использовать Zod схемы на фронте, но не забывать, что бэкенд (PHP) сейчас валидирует слабо. В будущем перенести `save.php` на FastAPI + Pydantic.
3. **Data Migration:**
   - *Решение:* Формат JSON файлов (`*.json`) не меняется, миграция данных не требуется.

---

## 📊 Статус выполнения (актуализировано 3.12.2025)

| Фаза | Статус | Прогресс | Приоритет |
|------|--------|----------|-----------|
| **Фаза 0: Инфраструктура** | ✅ Завершена | 100% | - |
| **Фаза 1: Миграция Реестра** | ✅ Завершена | 100% | - |
| **Фаза 2: Редактор КП** | 🔄 В процессе | ~90% | Высокий |
| **Фаза 3: Тендеры и PDF** | 🔄 В процессе | ~50% | Средний |
| **Фаза 4: Полировка и QA** | ❌ Не начата | 0% | Высокий |

### Критические задачи для завершения:
1. ✅ Автосохранение черновиков (Фаза 2) — **выполнено**
2. ✅ Автокомплит ИНН и адресов (Фаза 2) — **выполнено**
3. ✅ Валидация шагов формы (Фаза 2) — **выполнено**
4. ❌ Split-screen PDF Preview (Фаза 3)
5. ❌ Deployment для SPA (Фаза 4)
6. ❌ Code splitting и lazy loading (Фаза 4)

> Подробный анализ: [PROGRESS_ANALYSIS.md](./PROGRESS_ANALYSIS.md)

---
**Автор:** AI Assistant (Refined)
