# 🔧 Настройка nginx и SSL для kp.uralreduktor.com

## ✅ Выполнено

1. ✅ Создана конфигурация nginx: `/etc/nginx/sites-available/kp.uralreduktor.com`
2. ✅ Создан скрипт автоматизации: `/var/www/kp/setup_nginx_ssl.sh`

## 🚀 Быстрая установка

Запустите скрипт автоматизации (требуются права sudo):

```bash
cd /var/www/kp
sudo ./setup_nginx_ssl.sh
```

Скрипт автоматически:
- Активирует сайт в nginx
- Проверит конфигурацию
- Перезагрузит nginx
- Установит SSL сертификаты через certbot
- Настроит автоматическое перенаправление HTTP → HTTPS

## 📋 Ручная установка (пошагово)

### Шаг 1: Активация сайта

```bash
sudo ln -sf /etc/nginx/sites-available/kp.uralreduktor.com /etc/nginx/sites-enabled/kp.uralreduktor.com
```

### Шаг 2: Проверка конфигурации

```bash
sudo nginx -t
```

Если есть ошибки, исправьте их перед продолжением.

### Шаг 3: Перезагрузка nginx

```bash
sudo systemctl reload nginx
```

### Шаг 4: Установка SSL сертификатов

**Важно:** Убедитесь, что домен `kp.uralreduktor.com` указывает на IP-адрес этого сервера (DNS настроен).

```bash
sudo certbot --nginx -d kp.uralreduktor.com -d www.kp.uralreduktor.com
```

Certbot автоматически:
- Получит сертификаты от Let's Encrypt
- Обновит конфигурацию nginx
- Настроит автоматическое перенаправление HTTP → HTTPS
- Настроит автообновление сертификатов

### Шаг 5: Финальная проверка

```bash
# Проверка конфигурации
sudo nginx -t

# Перезагрузка nginx
sudo systemctl reload nginx

# Проверка статуса
sudo systemctl status nginx

# Проверка сертификатов
sudo certbot certificates
```

## 🔍 Проверка работы

### Проверка HTTP (должно перенаправлять на HTTPS)
```bash
curl -I http://kp.uralreduktor.com
```

### Проверка HTTPS
```bash
curl -I https://kp.uralreduktor.com
```

### Проверка в браузере
Откройте в браузере: `https://kp.uralreduktor.com`

## 📁 Структура конфигурации

- **Конфигурация:** `/etc/nginx/sites-available/kp.uralreduktor.com`
- **Активация:** `/etc/nginx/sites-enabled/kp.uralreduktor.com` (симлинк)
- **Директория сайта:** `/var/www/kp`
- **Логи:** 
  - `/var/log/nginx/kp.uralreduktor.com.access.log`
  - `/var/log/nginx/kp.uralreduktor.com.error.log`
- **SSL сертификаты:** `/etc/letsencrypt/live/kp.uralreduktor.com/`

## ⚙️ Особенности конфигурации

- ✅ Поддержка PHP 8.4 (FastCGI)
- ✅ Обработка API в `/api/`
- ✅ Gzip сжатие
- ✅ Кэширование статических файлов:
  - Изображения: 1 год
  - Шрифты: 1 год
  - CSS/JS: 30 дней
  - HTML: без кэширования
- ✅ Защита от доступа к скрытым файлам
- ✅ HTTP/2 поддержка (после установки SSL)

## 🔄 Обновление сертификатов

Сертификаты Let's Encrypt обновляются автоматически через systemd timer.

Проверка автообновления:
```bash
sudo systemctl status certbot.timer
```

Ручное обновление:
```bash
sudo certbot renew
```

## 🛠️ Troubleshooting

### Проблема: "nginx: configuration file test failed"
**Решение:** Проверьте синтаксис конфигурации:
```bash
sudo nginx -t
```

### Проблема: "Domain does not point to this server"
**Решение:** Убедитесь, что DNS настроен правильно:
```bash
dig kp.uralreduktor.com
nslookup kp.uralreduktor.com
```

### Проблема: "403 Forbidden"
**Решение:** Проверьте права доступа:
```bash
sudo chown -R www-data:www-data /var/www/kp
sudo chmod -R 755 /var/www/kp
```

### Проблема: PHP файлы не работают
**Решение:** Проверьте, что PHP-FPM запущен:
```bash
sudo systemctl status php8.4-fpm
```

### Проблема: Сертификаты не обновляются
**Решение:** Проверьте логи certbot:
```bash
sudo journalctl -u certbot.timer
sudo certbot renew --dry-run
```

## 📝 Примечания

- Конфигурация использует PHP 8.4 FPM
- Для изменения версии PHP измените путь к сокету в конфигурации
- Логи доступны в `/var/log/nginx/`
- После изменений конфигурации всегда проверяйте: `sudo nginx -t`

---

**Дата создания:** 2025-11-21  
**Версия:** 1.0

