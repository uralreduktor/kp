# 🔐 Парольная защита сайта kp.uralreduktor.com

## ✅ Выполнено

1. ✅ Создан файл с паролями: `/var/www/kp/.htpasswd`
2. ✅ Добавлена парольная защита в конфигурацию nginx
3. ✅ Настроены учетные данные:
   - **Логин**: `admin` - **Пароль**: `@kp2025#` (полный доступ)
   - **Логин**: `SidorkinV` - **Пароль**: `Svl@kp2025` (создание и редактирование, без удаления)

## 🚀 Применение конфигурации

Запустите скрипт автоматизации:

```bash
cd /var/www/kp
sudo ./setup_password.sh
```

Скрипт автоматически:
- Установит права на файл с паролями
- Проверит конфигурацию nginx
- Перезагрузит nginx
- Проверит работу парольной защиты

## 📋 Ручная установка

### Шаг 1: Установить права на файл с паролями

```bash
sudo chown www-data:www-data /var/www/kp/.htpasswd
sudo chmod 644 /var/www/kp/.htpasswd
```

### Шаг 2: Проверить конфигурацию nginx

```bash
sudo nginx -t
```

### Шаг 3: Перезагрузить nginx

```bash
sudo systemctl reload nginx
```

## 🧪 Тестирование

### Через браузер:
1. Откройте: `https://kp.uralreduktor.com`
2. Должно появиться окно авторизации
3. Введите: логин `admin`, пароль `@kp2025#`
4. Получите доступ к сайту

### Через curl:

```bash
# Без авторизации - должна быть ошибка 401
curl -I https://kp.uralreduktor.com

# С авторизацией - должен быть код 200
curl -I -u admin:"@kp2025#" https://kp.uralreduktor.com
```

## 📋 Добавленная конфигурация в nginx

```nginx
# Парольная защита всего сайта
auth_basic "KP Access - Restricted";
auth_basic_user_file /var/www/kp/.htpasswd;

# Для Let's Encrypt (исключаем из авторизации)
location /.well-known/acme-challenge/ {
    root /var/www/html;
    auth_basic off;
}
```

## 🔄 Изменение пароля

### Метод 1: Используя htpasswd

```bash
sudo htpasswd /var/www/kp/.htpasswd admin
# Введите новый пароль при запросе
sudo chown www-data:www-data /var/www/kp/.htpasswd
sudo chmod 644 /var/www/kp/.htpasswd
sudo systemctl reload nginx
```

### Метод 2: Используя openssl

```bash
cd /var/www/kp
echo -n "admin:" > .htpasswd
openssl passwd -apr1 "новый_пароль" >> .htpasswd
sudo chown www-data:www-data .htpasswd
sudo chmod 644 .htpasswd
sudo systemctl reload nginx
```

## 👥 Добавление нового пользователя

### Шаг 1: Добавить пользователя в .htpasswd

```bash
sudo htpasswd /var/www/kp/.htpasswd новый_пользователь
sudo chown www-data:www-data /var/www/kp/.htpasswd
sudo chmod 644 /var/www/kp/.htpasswd
sudo systemctl reload nginx
```

### Шаг 2: Настроить права доступа

Отредактируйте файл `/var/www/kp/api/permissions.json` и добавьте пользователя с нужными правами:

```json
{
  "users": {
    "новый_пользователь": {
      "canCreate": true,
      "canEdit": true,
      "canDelete": false,
      "description": "Описание прав пользователя"
    }
  }
}
```

**Права доступа:**
- `canCreate` - может создавать новые коммерческие предложения
- `canEdit` - может редактировать существующие коммерческие предложения
- `canDelete` - может удалять коммерческие предложения

**Текущие пользователи:**
- `admin` - полный доступ (создание, редактирование, удаление)
- `SidorkinV` - создание и редактирование, без удаления

## 🛠️ Troubleshooting

### Проблема: "401 Unauthorized" даже с правильным паролем

**Решение**: Проверьте:
1. Путь к файлу паролей в конфигурации nginx
2. Права доступа к файлу `.htpasswd`
3. Формат файла (должен быть: `username:hashed_password`)

```bash
# Проверка файла
cat /var/www/kp/.htpasswd

# Проверка прав
ls -la /var/www/kp/.htpasswd
```

### Проблема: "403 Forbidden"

**Решение**: Проверьте права доступа к файлам:

```bash
sudo chown -R www-data:www-data /var/www/kp
sudo chmod -R 755 /var/www/kp
```

### Проблема: Сайт доступен без авторизации

**Решение**: 
1. Проверьте, что `auth_basic` добавлен в конфигурацию
2. Убедитесь, что nginx перезагружен: `sudo systemctl reload nginx`
3. Проверьте логи: `sudo tail -f /var/log/nginx/kp.uralreduktor.com.error.log`

### Проблема: Статические файлы (CSS, JS) не загружаются

**Решение**: Это нормально - все файлы защищены паролем. Браузер автоматически передаст авторизацию при запросе статических файлов после успешной авторизации на главной странице.

## 📝 Примечания

- Парольная защита применяется ко всему сайту, включая API
- Исключение сделано только для `.well-known/acme-challenge/` (для обновления SSL сертификатов)
- Файл `.htpasswd` использует формат Apache htpasswd (совместим с nginx)
- Пароли хранятся в зашифрованном виде (apr1 hash)

---

## 🔐 Система прав доступа

Система поддерживает гибкое управление правами пользователей через файл `/var/www/kp/api/permissions.json`.

### Файлы системы прав:
- `/var/www/kp/.htpasswd` - файл с паролями пользователей (HTTP Basic Auth)
- `/var/www/kp/api/permissions.json` - файл с правами доступа пользователей
- `/var/www/kp/api/auth_utils.php` - PHP утилиты для проверки прав
- `/var/www/kp/api/user_info.php` - API endpoint для получения информации о текущем пользователе

### Как работает система прав:
1. Пользователь авторизуется через HTTP Basic Auth (nginx)
2. PHP получает имя пользователя из `$_SERVER['REMOTE_USER']`
3. Права пользователя загружаются из `permissions.json`
4. API endpoints проверяют права перед выполнением действий
5. Интерфейс скрывает кнопки для действий, на которые у пользователя нет прав

### Проверка прав в API:
- `delete.php` - проверяет право `canDelete` перед удалением
- Другие endpoints могут быть расширены аналогично

---

**Дата создания**: 2025-11-21  
**Дата обновления**: 2025-11-21 (добавлена система прав доступа)  
**Версия**: 2.0

