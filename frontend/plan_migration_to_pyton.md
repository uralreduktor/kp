# План миграции PHP → Python (FastAPI)

Я провел анализ вашего проекта, чтобы составить план отказа от PHP в пользу Python (FastAPI).

## Текущее состояние (As-Is)

Проект находится в "гибридном" состоянии:

1. **Frontend**: React (Vite), строго типизирован, использует Zod.
2. **Backend (Legacy)**: PHP-скрипты (`api/*.php`). Отвечают за CRUD операции с файлами (сохранение/чтение JSON), генерацию номеров, PDF и проксирование запросов (DaData).  
   *Проблема*: Отсутствие строгой типизации, ручное управление файлами, конфликты типов данных (массив `[]` vs объект `{}`).
3. **Backend (Modern)**: Python FastAPI (`backend/app`). Отвечает за авторизацию (Postgres) и парсинг (Playwright).  
   *Потенциал*: Уже настроен, типизирован (Pydantic), работает асинхронно.

## Целевое состояние (To-Be)

Единый бэкенд на FastAPI. PHP полностью исключен из цепочки обработки данных.

* **Данные**: Инвойсы по-прежнему хранятся в JSON файлах (для обратной совместимости на первом этапе), но управляются через Python.
* **API**: RESTful эндпоинты `/api/v1/*`.

---

## План миграции (Скорректированный)

Я разбил план на 5 этапов, добавив критические проверки безопасности и совместимости.

### Этап 1: Описание данных (Schemas) с валидацией Legacy ✅ ЗАВЕРШЁН

*Сделано:*  
— Pydantic-схемы `Invoice`, `InvoiceItem` и др. поддерживают legacy: `[]→{}`, alias `_metadata`, coercion чисел (строки/NULL→float), nullable поля.  
— Покрыто pytest: коэрсинг чисел, alias `_metadata`, `[]→{}`.  
— Схемы расширены для поддержки всех полей из legacy JSON (коммерческие условия, контакты, спецификации редукторов).  
— Добавлена поддержка top-level полей для legacy совместимости (`incoterm`, `contactPerson`, и т.д.).

*Осталось:*  
— Финальная ревизия схем под прод-данные после полного отказа от PHP (низкий приоритет).

### Этап 2: Логика работы с файлами (Service Layer) и Права ✅ ЗАВЕРШЁН

*Сделано:*  
— Сохранение/загрузка/удаление через FastAPI (`/api/v1/invoices`), atomic write, json cleaning, алиасы.  
— Генерация номера с учётом legacy формата `DDMMYY-NN`.  
— Реализован полный CRUD: `GET /api/v1/invoices` (список), `GET /api/v1/invoices/{filename}` (загрузка), `POST /api/v1/invoices/` (сохранение), `DELETE /api/v1/invoices/{filename}` (удаление).  
— Atomic write с временными файлами для безопасного сохранения.

*Осталось:*  
— Проверка/настройка прав на `@archiv 2025`, резервная копия перед финальным отключением PHP.  
— UI для удаления КП через фронтенд (кнопка в списке).

### Этап 3: Перенос сопутствующих сервисов (DaData, PDF) ✅ ЗАВЕРШЁН

PHP также отвечал за подсказки и PDF. Оба сервиса успешно мигрированы.

*Сделано:*  
— **DaData**: Подсказки работают через `/api/v1/suggest/party` и `/api/v1/suggest/address`.  
— **Импорт тендера**: Автоподтягивание адреса из DaData по ИНН, парсинг позиций с B2B-Center (включая `/positions/` страницы).  
— **PDF на Python**: Полностью переписан на Jinja2 + Playwright:
  - Парсинг данных организаций из `js/organizations.js` (реплика PHP-логики).
  - Конвертация изображений (логотип, печать, подпись) в base64.
  - Форматирование чисел с пробелами (`1 000 000,00`), целые числа для "шт".
  - Форматирование дат в русский формат.
  - Полный шаблон с таблицей товаров, коммерческими условиями, реквизитами, подписью.
  - Динамическая настройка отступов подписи в зависимости от количества позиций.
  - Корректная обработка вложенных объектов `commercialTerms` и `contact` без потери данных.
— **Эндпоинт**: `GET /api/v1/invoices/{filename}/pdf` возвращает PDF напрямую.

*Осталось:*  
— Убрать DEBUG-логи из `pdf_service.py`.  
— Очистить legacy прокси для подсказок в PHP (после финального тестирования).  
— Опционально: техническое приложение в PDF (если требуется).

### Этап 4: Роутинг и Nginx ⚠️ В ПРОЦЕССЕ

*Сделано:*  
— Версионированные маршруты `/api/v1/*` с защитой `SessionGuard/DeviceGuard`.  
— Парсинг `/api/parsing/tender` возвращает ИНН, номер торга, адрес, позиции (с поддержкой многостраничных тендеров).  
— Все основные эндпоинты работают:
  - `GET /api/v1/invoices` — список КП
  - `GET /api/v1/invoices/{filename}` — загрузка КП
  - `POST /api/v1/invoices/` — сохранение КП
  - `DELETE /api/v1/invoices/{filename}` — удаление КП
  - `GET /api/v1/invoices/{filename}/pdf` — генерация PDF
  - `POST /api/v1/suggest/party` — подсказки ИНН
  - `POST /api/v1/suggest/address` — подсказки адресов
  - `GET /api/parsing/tender` — парсинг тендеров

*Осталось:*  
— **Критично**: Финальная чистка Nginx — отключить PHP-фолбэки для инвойсов/PDF после валидации.  
— Добавить rate-limit на auth/сохранение при необходимости.  
— Проверить, что все старые PHP эндпоинты (`api/save_invoice.php`, `api/load_invoice.php`, `api/invoice_html_generator.php`) больше не используются.

### Этап 5: Интеграция на Фронтенде ⚠️ В ПРОЦЕССЕ

*Сделано:*  
— Все invoice и suggest эндпоинты переведены на `/api/v1/`.  
— Импорт тендера (B2B-Center):
  - Загружает ИНН, номер торга, адрес (с автоподтягиванием из DaData).
  - Парсит позиции товаров со страницы `/positions/` без потери данных получателя.
  - Dark mode поддержка в формах.
— Формы очищены от лишних legacy-препроцессоров.  
— PDF preview/download работает через FastAPI (`/api/v1/invoices/{filename}/pdf`).  
— Исправлены ошибки валидации Zod для legacy данных (null ID товаров, пустые объекты).

*Осталось:*  
— Финальный рефактор Zod-схем под контракт FastAPI (убрать временные касты, если они остались).  
— UI для удаления КП (кнопка в реестре).  
— UI/UX: индикаторы загрузки/ошибок парсинга.  
— Тесты: unit (msw для API мокирования), e2e (Playwright для критических флоу).

---

## ✅ Что завершено

### Критические компоненты (100% готовы к отключению PHP)

1. **CRUD для инвойсов** — полностью на FastAPI:
   - Список, загрузка, сохранение, удаление через `/api/v1/invoices`.
   - Atomic write, генерация номеров в формате `DDMMYY-NN`.
   - Поддержка всех legacy форматов JSON.

2. **PDF генерация** — чистый Python (Jinja2 + Playwright):
   - Полное соответствие дизайну продакшн PHP-шаблона.
   - Форматирование чисел (`1 000 000,00`), даты (русский формат).
   - Таблица товаров с 7 колонками, итоги, НДС.
   - Коммерческие условия, гарантия, примечания.
   - Банковские реквизиты, подпись, печать.
   - Эндпоинт: `GET /api/v1/invoices/{filename}/pdf`.

3. **DaData интеграция**:
   - Подсказки ИНН/адресов через `/api/v1/suggest/*`.
   - Автоподтягивание адреса по ИНН при импорте тендера.

4. **Парсинг тендеров (B2B-Center)**:
   - Извлечение ИНН, номера торга, наименования получателя.
   - Парсинг позиций со страницы `/positions/`.
   - Playwright с stealth-плагинами для обхода защиты.

5. **Pydantic схемы**:
   - Поддержка всех legacy форматов (`[]→{}`, nullable, string→number).
   - Unit-тесты (pytest) для валидации.

---

## 🔄 Что в процессе

1. **Nginx конфигурация**:
   - Нужно проверить и отключить PHP-фолбэки для `/api/invoices`, `/api/pdf`.
   - Убедиться, что все запросы идут на FastAPI (порт 8001).

2. **Frontend доработки**:
   - Добавить UI для удаления КП в реестре.
   - Улучшить индикаторы загрузки/ошибок при импорте тендера.
   - Финальная проверка Zod-схем (убрать лишние преобразования, если остались).

3. **Тестирование**:
   - Ручная проверка всех критических сценариев:
     - ✅ Создание КП
     - ✅ Редактирование КП
     - ✅ Просмотр КП
     - ✅ Генерация PDF
     - ✅ Импорт тендера (ИНН, адрес, позиции)
     - ⚠️ Удаление КП (бэкенд готов, UI — нет)
   - Автоматизированные тесты (опционально, для CI/CD).

---

## 🎯 Следующие шаги (приоритеты)

### Высокий приоритет (для production-ready)

1. **Убрать DEBUG-логи из PDF Service**:
   - Очистить `print()` из `backend/app/services/pdf_service.py`.
   - Проверить, что нет лишнего логирования в production.

2. **Nginx cleanup**:
   - Проверить конфигурацию `/etc/nginx/sites-available/kp`.
   - Убедиться, что `location /api/` не проксирует на PHP для инвойсов/PDF.
   - Оставить PHP только для legacy-эндпоинтов (если они ещё нужны).

3. **Backup и проверка прав**:
   - Создать резервную копию `@archiv 2025` перед финальным переходом.
   - Проверить права доступа FastAPI к файлам (чтение/запись).

4. **UI для удаления КП**:
   - Добавить кнопку "Удалить" в реестр КП (`RegistryTable.tsx`).
   - Подтверждение перед удалением (диалог).

### Средний приоритет (улучшения)

5. **Индикаторы загрузки**:
   - Спиннер при импорте тендера.
   - Прогресс при генерации PDF (если операция долгая).

6. **Rate limiting**:
   - Добавить ограничение на частоту сохранения КП (защита от DDOS).
   - Ограничение на парсинг (не более N запросов в минуту).

7. **Мониторинг и логирование**:
   - Настроить structured logging (JSON) для FastAPI.
   - Интегрировать с Sentry/Prometheus (если есть).

### Низкий приоритет (опционально)

8. **Техническое приложение в PDF**:
   - Если требуется, добавить раздел с характеристиками редукторов.
   - Генерация многостраничных PDF с оглавлением.

9. **Миграция на Postgres**:
   - Переход с JSON-файлов на БД для инвойсов (долгосрочная задача).
   - Создать таблицы `invoices`, `invoice_items`, миграции Alembic.

10. **E2E тесты**:
    - Playwright для критических флоу: создание → редактирование → PDF → удаление.
    - Интеграция в CI/CD (GitHub Actions).

---

## 📋 Чек-лист готовности к отключению PHP

Перед тем как полностью выключить PHP-бэкенд для инвойсов, проверь:

- [ ] ✅ CRUD инвойсов работает через `/api/v1/invoices` (список, загрузка, сохранение, удаление).
- [ ] ✅ PDF генерируется через Python (`/api/v1/invoices/{filename}/pdf`), соответствует продакшн-дизайну.
- [ ] ✅ Импорт тендера: ИНН, адрес (DaData), позиции (парсинг `/positions/`).
- [ ] ✅ Старые КП из `@archiv 2025` открываются без ошибок (legacy-совместимость).
- [ ] ⚠️ Nginx не проксирует `/api/invoices` и `/api/pdf` на PHP.
- [ ] ⚠️ Создан backup `@archiv 2025` перед переходом.
- [ ] ⚠️ Проверены права FastAPI на чтение/запись файлов.
- [ ] ⚠️ DEBUG-логи убраны из production-кода.
- [ ] ❌ UI для удаления КП в реестре (опционально, можно сделать после).

---

## 🚀 Рекомендация

**На данный момент**: 
- Критические компоненты (CRUD, PDF, DaData, парсинг) готовы на **95%**.
- Основная работа — это **инфраструктурная подготовка** (Nginx, backup, cleanup) и **финальное тестирование**.

**Следующий шаг**:
1. Убрать DEBUG-логи из `pdf_service.py`.
2. Проверить Nginx конфигурацию.
3. Создать backup `@archiv 2025`.
4. Провести финальное тестирование всех сценариев (создание, редактирование, PDF, импорт).
5. Отключить PHP-эндпоинты для инвойсов/PDF в Nginx.

После этого можно считать миграцию **завершённой** для инвойсов и PDF. 🎉
